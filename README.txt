FocusToCursor
An addon for blender that sets the focal point of the active camera to the 3D cursor.

INSTALLATION:
In Blender go to File > User Preferences > Addons > Install from file, then navigate to "focusToCursor.py" and install it.
